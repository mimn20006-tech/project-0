{
  "name": "hand-aura-mobile",
  "private": true,
  "version": "1.0.0",
  "description": "Capacitor wrapper for Hand Aura store",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "ios": "npx cap open ios",
    "copy": "npx cap copy"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  }
}
